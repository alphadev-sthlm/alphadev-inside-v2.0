{
  "name": "time-report-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predev": "rimraf public",
    "nodemon": "nodemon",
    "build:styles": "node scripts/buildStyles",
    "build": "mkdir -p public & npm run build:styles",
    "watch:styles": "node scripts/buildStyles -w 'assets/styles/**/*.styl'",
    "watch": "npm run watch:styles",
    "dev": "mkdir -p public & run-p watch nodemon",
    "start": "npm run build & node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "express-handlebars": "^3.0.0",
    "log": "^1.4.0",
    "stylus": "^0.54.5",
    "morgan": "^1.9.0",
    "chokidar": "^2.0.4"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2"
  }
}
